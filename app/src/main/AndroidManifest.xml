<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unic.unic_vendor_final_1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.SelectView" />
        <activity android:name=".views.SetShopStructure" />
        <activity android:name=".views.AddShop" />
        <activity android:name=".views.Login" />
        <activity android:name=".views.SignUp" />
        <activity android:name=".views.UserHome" />
        <activity
            android:name=".views.Welcome"
            android:screenOrientation="portrait" />
        <activity android:name=".views.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>